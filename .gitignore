# ---- Python ----
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage
*.egg-info/

# ---- 数据（不入库）----
data/*.sqlite3
data/*.sqlite3-wal
data/*.sqlite3-shm
data/*.duckdb
data/raw/
data/normalized/
data/factors/
data/backtests/

# ---- 前端 ----
apps/web/node_modules/
apps/web/.next/
apps/web/out/
apps/web/next-env.d.ts
apps/web/test-results/
apps/web/playwright-report/
*.tsbuildinfo

# ---- 截图复刻产物（保留 notes.md，忽略大图）----
apps/web/artifacts/ui-review/*/current.png
apps/web/artifacts/ui-review/*/reference.png

# ---- 生成产物（验收 JSON / 日志 / 截图）----
# 这些是可重新生成的验证输出，不入库；文档在 docs/ 下。
artifacts/
services/ziwei-service/node_modules/
services/ziwei-service/dist/
services/ziwei-reference-service/node_modules/
services/ziwei-reference-service/package-lock.json

# ---- 杂项 ----
# 临时响应抓取文件（不属于项目交付物）
resp_an.json
.env
.DS_Store
Thumbs.db
*.log

# ---- Phase 3D 面板缓存（可由 scripts/phase3d_collect_panel.py 重新生成）----
data/phase3_universe/phase3d_cache/

# ---- Phase 4 供应商 Parquet 仓库（可由 scripts/phase4_import_vendor.py 重建，1.2G 不入库）----
data/vendor/

# ---- Phase 4 面板采集缓存（可重建；一次性 5–6 小时，按需保留）----
data/phase4_cache/

# ---- Phase 4 标签缓存（可由 scripts/phase4_neutralization.py 重建，15MB 不入库）----
data/phase4_universe/phase4_label_cache.parquet

# ---- Playwright MCP 临时产物 ----
.playwright-mcp/

# ---- 本地检查产物（截图 / 验证日志 / 一次性脚本；可重新生成，不入库）----
output/
