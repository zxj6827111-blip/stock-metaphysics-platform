# 紫微因子字典（Phase 2B）

> 本文件由 `scripts/ziwei_factor_quality_audit.py` **自动生成**，请勿手工编辑。
> 规则版本：定义 `zv1`，观测按 variant 后缀区分为 `zv1.fwd` / `zv1.rev`。
> 引擎版本：`iztro-2.6.1+smx-1.0.0`。

---

## 0. 先读这一段

### 0.1 宫位→金融含义的映射不是传统定论

「财帛宫 = 资金/价格」「官禄宫 = 公司经营」「迁移宫 = 外部市场」**不是传统紫微斗数的规定**，而是本项目的**研究假设**，版本号 `ziwei_stock_mapping_v1`，必须先经历史回测检验。
禁止在 UI / 文档 / 报告中表述为「传统紫微规定财帛宫就是股价」。

### 0.2 方向不是收益率

每个因子的 `direction` / `rule_score` 表达的是**传统规则认为的方向与强度**：

* 吉曜 / 庙旺 / 化禄权科 → 传统视为偏吉（`POSITIVE`）
* 煞曜 / 化忌 → 传统视为偏凶（`NEGATIVE`）
* 宫位位阶 / 主星数量 / 五行局 → 结构性变量（`NEUTRAL`）

它们**都不是预期收益率，也不是上涨概率**。

### 0.3 关键宫（研究映射的位阶体系）

核心宫（位阶 2）：命宫、财帛、官禄、迁移；次核心宫（位阶 1）：福德、田宅；其余位阶 0。

### 0.4 随 variant 变化的因子

只有以下因子会随顺行/逆行 variant 变化（4 个）：`Z_DECADE_001`、`Z_DECADE_002`、`Z_AGE_001`、`Z_AGE_002`。

其余因子在两个 variant 之间**完全一致** —— 因为 variant 只影响大限/小限顺逆与长生十二神顺逆（见 `docs/calculation-differences-phase2-ziwei.md` D4）。
**两个 variant 不是两条独立证据。**

---

## 1. 因子总览（49 个）

| 命名空间 | 数量 | 含义 |
|---|---|---|
| `Z_AGE_*` | 2 | 小限（随 variant 变化） |
| `Z_CAREER_*` | 4 | 官禄宫（研究映射：公司经营） |
| `Z_DAY_*` | 5 | 流日（周度聚合输入） |
| `Z_DECADE_*` | 2 | 大限（随 variant 变化） |
| `Z_FIN_*` | 4 | 财帛宫（研究映射：资金/价格结构） |
| `Z_LIFE_*` | 7 | 命宫 |
| `Z_MONTH_*` | 5 | 流月 |
| `Z_MOVE_*` | 3 | 迁移宫（研究映射：外部市场） |
| `Z_MUTAGEN_*` | 6 | 生年四化 |
| `Z_TRINE_*` | 5 | 三方四正 |
| `Z_YEAR_*` | 6 | 流年 |

---

## 2. 逐因子定义

### Z_AGE_* · 小限（随 variant 变化）

#### `Z_AGE_001` · 当前小限宫主星庙旺和

* **方向（传统规则）**：正向
* **原始值单位**：`score_sum`
* **归一化**：tanh(sum / 1.5)
* **依赖盘面字段**：`ziwei.horoscope.age.index`
* **标签**：紫微, 小限, variant敏感
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

as_of 所处小限所在宫位的主星庙旺等级之和。**随 variant 变化。**

**计算**

```
sum(BRIGHTNESS_SCORE over 小限宫主星)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.83 | 20 | 0.404558 | 0.327156 | — |

#### `Z_AGE_002` · 当前小限宫煞曜数量

* **方向（传统规则）**：负向
* **原始值单位**：`count`
* **归一化**：-min(count / 2, 1)
* **依赖盘面字段**：`ziwei.horoscope.age.index`
* **标签**：紫微, 小限, variant敏感
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

as_of 所处小限所在宫位的煞曜数量。**随 variant 变化。**注意：小限层在 iztro 中不提供流曜，因此本因子只使用原局煞曜。

**计算**

```
count(原局煞曜 in 小限宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.4 | 3 | -0.2475 | 0.332485 | — |

### Z_CAREER_* · 官禄宫（研究映射：公司经营）

#### `Z_CAREER_001` · 官禄宫主星庙旺和

* **方向（传统规则）**：正向
* **原始值单位**：`score_sum`
* **归一化**：tanh(sum / 1.5)
* **依赖盘面字段**：`ziwei.palace.官禄.major_stars`
* **标签**：紫微, 官禄宫, research_mapping
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

官禄宫主星庙旺等级之和。**研究映射**：官禄宫 → 公司经营/行业地位，属本项目假设。

**计算**

```
sum(BRIGHTNESS_SCORE[主星.brightness] for 主星 in 官禄宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.8 | 10 | 0.398323 | 0.329214 | — |

#### `Z_CAREER_002` · 官禄宫主星数量

* **方向（传统规则）**：中性
* **原始值单位**：`count`
* **归一化**：min(count / 2, 1)
* **依赖盘面字段**：`ziwei.palace.官禄`
* **标签**：紫微, 官禄宫
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

官禄宫主星个数（0 为空宫）。结构性变量，方向中性。

**计算**

```
len(官禄宫.major_stars)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.8 | 3 | 0.575 | 0.364057 | — |

#### `Z_CAREER_003` · 官禄宫煞曜数量

* **方向（传统规则）**：负向
* **原始值单位**：`count`
* **归一化**：-min(count / 2, 1)
* **依赖盘面字段**：`ziwei.palace.官禄`
* **标签**：紫微, 官禄宫, 煞曜
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

官禄宫中六煞星的数量。**研究映射**：官禄宫 → 公司经营，故该因子被用作『经营阻力』的代理变量，属本项目假设。**不是传统定论。**

**计算**

```
count(malefic in 官禄宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.45 | 3 | -0.275 | 0.335317 | — |

#### `Z_CAREER_004` · 官禄宫化权化科同宫

* **方向（传统规则）**：正向
* **原始值单位**：`bool`
* **归一化**：True→+1，False→0
* **依赖盘面字段**：`ziwei.natal_mutagens`
* **标签**：紫微, 官禄宫, 四化
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

生年化权或化科是否落在官禄宫。传统视为权位/名位受化。

**计算**

```
any(生年四化 in {权, 科} 落于 官禄宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.15 | 2 | 0.15 | 0.357967 | — |

### Z_DAY_* · 流日（周度聚合输入）

#### `Z_DAY_001` · 流日命宫原盘位阶

* **方向（传统规则）**：中性
* **原始值单位**：`palace_index`
* **归一化**：index 映射到 [-1,1]；仅作结构编码
* **依赖盘面字段**：`ziwei.horoscope.daily.index`
* **标签**：紫微, 流日, 周度
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

该日流日命宫落在原盘的哪一宫。结构变量，方向中性。本因子是周度聚合（交易日流日 → 周）的基础输入。

**计算**

```
horoscope.daily.index
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 12 | 0.023636 | 0.606541 | — |

#### `Z_DAY_002` · 流日命宫主星庙旺和

* **方向（传统规则）**：正向
* **原始值单位**：`score_sum`
* **归一化**：tanh(sum / 1.5)
* **依赖盘面字段**：`ziwei.horoscope.daily.index`
* **标签**：紫微, 流日
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流日命宫（原盘对应宫位）的主星庙旺等级之和。

**计算**

```
sum(BRIGHTNESS_SCORE over 流日命宫主星)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.85 | 16 | 0.396097 | 0.322401 | — |

#### `Z_DAY_003` · 流日命宫煞曜数量

* **方向（传统规则）**：负向
* **原始值单位**：`count`
* **归一化**：-min(count / 3, 1)
* **依赖盘面字段**：`ziwei.horoscope.daily`
* **标签**：紫微, 流日, 煞曜
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流日命宫所在宫位的原局煞曜与流日煞曜总数。

**计算**

```
count(煞曜)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.47 | 3 | -0.196667 | 0.231977 | — |

#### `Z_DAY_004` · 流日化禄落原盘宫

* **方向（传统规则）**：中性
* **原始值单位**：`palace_index`
* **归一化**：index 映射到 [-1,1]
* **依赖盘面字段**：`ziwei.horoscope.daily.mutagen`
* **标签**：紫微, 流日, 四化
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流日四化中化禄所落星曜的原盘宫位 index。结构变量，方向中性。

**计算**

```
horoscope.daily.mutagen[0] → 星曜原盘落宫
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 11 | 0.061818 | 0.632847 | — |

#### `Z_DAY_005` · 流日化忌落原盘宫

* **方向（传统规则）**：中性
* **原始值单位**：`palace_index`
* **归一化**：index 映射到 [-1,1]
* **依赖盘面字段**：`ziwei.horoscope.daily.mutagen`
* **标签**：紫微, 流日, 四化
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流日四化中化忌所落星曜的原盘宫位 index。结构变量，方向中性。

**计算**

```
horoscope.daily.mutagen[3] → 星曜原盘落宫
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 11 | -0.178182 | 0.660468 | — |

### Z_DECADE_* · 大限（随 variant 变化）

#### `Z_DECADE_001` · 当前大限宫主星庙旺和

* **方向（传统规则）**：正向
* **原始值单位**：`score_sum`
* **归一化**：tanh(sum / 1.5)
* **依赖盘面字段**：`ziwei.horoscope.decadal.index`
* **标签**：紫微, 大限, variant敏感
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

as_of 所处大限所在宫位的主星庙旺等级之和。**该因子随 variant（顺行/逆行）变化**，是两个 variant 对比的核心变量。

**计算**

```
sum(BRIGHTNESS_SCORE over 大限宫主星)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.755 | 15 | 0.316483 | 0.341944 | — |

#### `Z_DECADE_002` · 当前大限宫煞曜数量

* **方向（传统规则）**：负向
* **原始值单位**：`count`
* **归一化**：-min(count / 2, 1)
* **依赖盘面字段**：`ziwei.horoscope.decadal.index`
* **标签**：紫微, 大限, variant敏感
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

as_of 所处大限所在宫位的煞曜数量。**随 variant 变化。**

**计算**

```
count(煞曜 in 大限宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.355 | 3 | -0.2025 | 0.292698 | — |

### Z_FIN_* · 财帛宫（研究映射：资金/价格结构）

#### `Z_FIN_001` · 财帛宫主星庙旺和

* **方向（传统规则）**：正向
* **原始值单位**：`score_sum`
* **归一化**：tanh(sum / 1.5)
* **依赖盘面字段**：`ziwei.palace.财帛.major_stars`
* **标签**：紫微, 财帛宫, research_mapping
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

财帛宫主星庙旺等级之和。**研究映射**：把财帛宫视为『资金/价格结构』的代理，该映射属本项目假设（ziwei_stock_mapping_v1），不是传统紫微对股票的规定。

**计算**

```
sum(BRIGHTNESS_SCORE[主星.brightness] for 主星 in 财帛宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.9 | 12 | 0.406619 | 0.375426 | — |

#### `Z_FIN_002` · 财帛宫主星数量

* **方向（传统规则）**：中性
* **原始值单位**：`count`
* **归一化**：min(count / 2, 1)
* **依赖盘面字段**：`ziwei.palace.财帛`
* **标签**：紫微, 财帛宫
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

财帛宫主星个数（0 为空宫）。结构性变量，方向中性。

**计算**

```
len(财帛宫.major_stars)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.9 | 3 | 0.675 | 0.327738 | — |

#### `Z_FIN_003` · 财帛宫煞曜数量

* **方向（传统规则）**：负向
* **原始值单位**：`count`
* **归一化**：-min(count / 2, 1)
* **依赖盘面字段**：`ziwei.palace.财帛`
* **标签**：紫微, 财帛宫, 煞曜
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

财帛宫中六煞星（擎羊/陀罗/火星/铃星/地空/地劫）的数量。传统视为财位上的阻力；**这是传统规则的看法，不构成对股价的判断。**

**计算**

```
count(malefic in 财帛宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.6 | 3 | -0.375 | 0.349982 | — |

#### `Z_FIN_004` · 财帛宫化禄化权同宫

* **方向（传统规则）**：正向
* **原始值单位**：`bool`
* **归一化**：True→+1，False→0
* **依赖盘面字段**：`ziwei.natal_mutagens`
* **标签**：紫微, 财帛宫, 四化
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

生年化禄或化权是否落在财帛宫。传统视为财位受化，偏吉。

**计算**

```
any(生年四化 in {禄, 权} 落于 财帛宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.25 | 2 | 0.25 | 0.434099 | — |

### Z_LIFE_* · 命宫

#### `Z_LIFE_001` · 命宫主星庙旺和

* **方向（传统规则）**：正向
* **原始值单位**：`score_sum`
* **归一化**：tanh(sum / 1.5)，落在 [-1, 1]
* **依赖盘面字段**：`ziwei.palace.命宫.major_stars`
* **标签**：紫微, 命宫, 庙旺
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

命宫主星的庙旺等级之和。传统紫微以主星庙旺程度衡量该宫的力量强弱。股票研究中作为『主体结构强度』的代理变量（研究假设，非定论）。

**计算**

```
sum(BRIGHTNESS_SCORE[主星.brightness] for 主星 in 命宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.85 | 11 | 0.39304 | 0.325403 | — |

#### `Z_LIFE_002` · 命宫主星数量

* **方向（传统规则）**：中性
* **原始值单位**：`count`
* **归一化**：min(count / 2, 1)（仅作强度，方向中性）
* **依赖盘面字段**：`ziwei.palace.命宫.major_stars`
* **标签**：紫微, 命宫
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

命宫主星个数。0 表示空宫（传统认为需借对宫安星），属于结构性变量，数量本身不对应收益方向。

**计算**

```
len(命宫.major_stars)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.85 | 3 | 0.6 | 0.339967 | — |

#### `Z_LIFE_003` · 命宫吉曜数量

* **方向（传统规则）**：正向
* **原始值单位**：`count`
* **归一化**：min(count / 3, 1)
* **依赖盘面字段**：`ziwei.palace.命宫`
* **标签**：紫微, 命宫, 吉曜
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

命宫中六吉星（左辅/右弼/文昌/文曲/天魁/天钺）与禄马（禄存/天马）的数量。传统视为助力，本项目仅作为可检验的结构变量。

**计算**

```
count(star_category ∈ {lucky, wealth_move} in 命宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.45 | 3 | 0.183333 | 0.223544 | — |

#### `Z_LIFE_004` · 命宫煞曜数量

* **方向（传统规则）**：负向
* **原始值单位**：`count`
* **归一化**：-min(count / 2, 1)
* **依赖盘面字段**：`ziwei.palace.命宫`
* **标签**：紫微, 命宫, 煞曜
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

命宫中六煞星（擎羊/陀罗/火星/铃星/地空/地劫）的数量。传统视为阻力。**这是传统规则的看法，不构成对股价的判断。**

**计算**

```
count(star_category == malefic in 命宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.45 | 3 | -0.25 | 0.296546 | — |

#### `Z_LIFE_005` · 命宫吉煞差

* **方向（传统规则）**：正向
* **原始值单位**：`count_diff`
* **归一化**：tanh(diff / 2)
* **依赖盘面字段**：`ziwei.palace.命宫`
* **标签**：紫微, 命宫
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

命宫吉曜数与煞曜数之差，传统『吉凶相抵』概念的量化近似。正值表示吉曜多于煞曜。这是传统规则的看法，不构成对股价的判断。

**计算**

```
(吉曜数 + 禄马数) - 煞曜数
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.5 | 5 | 0.014974 | 0.402908 | — |

#### `Z_LIFE_006` · 身宫与命宫同宫

* **方向（传统规则）**：中性
* **原始值单位**：`bool`
* **归一化**：True→+0.5、False→0（仅作结构标记）
* **依赖盘面字段**：`ziwei.body_palace_index`
* **标签**：紫微, 命宫, 身宫
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

身宫是否与命宫落在同一宫位。传统认为命身同宫者结构集中、倾向性更强。作为结构性标记，方向中性。

**计算**

```
body_palace_index == soul_palace_index
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.0 | 1 | 0.0 | 0.0 | `CONSTANT_FACTOR`、`LOW_DISCRIMINATION_FACTOR` |

#### `Z_LIFE_007` · 五行局

* **方向（传统规则）**：中性
* **原始值单位**：`class`
* **归一化**：局数 2..6 线性映射到 [-1,1]；分类变量仅作结构编码
* **依赖盘面字段**：`ziwei.five_elements_class`
* **标签**：紫微, 五行局
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

命宫所属五行局（水二/木三/金四/土五/火六），决定起运岁数与紫微星安放起点。属于分类变量，不映射收益方向。

**计算**

```
iztro five_elements_class
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.7 | 5 | 0.225 | 0.559859 | — |

### Z_MONTH_* · 流月

#### `Z_MONTH_001` · 流月命宫原盘位阶

* **方向（传统规则）**：中性
* **原始值单位**：`palace_index`
* **归一化**：index 映射到 [-1,1]；仅作结构编码
* **依赖盘面字段**：`ziwei.horoscope.monthly.index`
* **标签**：紫微, 流月
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

该月流月命宫落在原盘的哪一宫。结构变量，方向中性。

**计算**

```
horoscope.monthly.index
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 12 | -0.034545 | 0.637541 | — |

#### `Z_MONTH_002` · 流月命宫主星庙旺和

* **方向（传统规则）**：正向
* **原始值单位**：`score_sum`
* **归一化**：tanh(sum / 1.5)
* **依赖盘面字段**：`ziwei.horoscope.monthly.index`
* **标签**：紫微, 流月
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流月命宫（原盘对应宫位）的主星庙旺等级之和。

**计算**

```
sum(BRIGHTNESS_SCORE over 流月命宫主星)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.82 | 17 | 0.379912 | 0.335373 | — |

#### `Z_MONTH_003` · 流月命宫煞曜数量

* **方向（传统规则）**：负向
* **原始值单位**：`count`
* **归一化**：-min(count / 3, 1)
* **依赖盘面字段**：`ziwei.horoscope.monthly`
* **标签**：紫微, 流月, 煞曜
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流月命宫所在宫位的原局煞曜与流月煞曜总数。

**计算**

```
count(煞曜)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.42 | 4 | -0.163333 | 0.213947 | — |

#### `Z_MONTH_004` · 流月化禄落原盘宫

* **方向（传统规则）**：中性
* **原始值单位**：`palace_index`
* **归一化**：index 映射到 [-1,1]
* **依赖盘面字段**：`ziwei.horoscope.monthly.mutagen`
* **标签**：紫微, 流月, 四化
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流月四化中化禄所落星曜的原盘宫位 index。结构变量，方向中性。

**计算**

```
horoscope.monthly.mutagen[0] → 星曜原盘落宫
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 12 | -0.061818 | 0.686243 | — |

#### `Z_MONTH_005` · 流月化忌落原盘宫

* **方向（传统规则）**：中性
* **原始值单位**：`palace_index`
* **归一化**：index 映射到 [-1,1]
* **依赖盘面字段**：`ziwei.horoscope.monthly.mutagen`
* **标签**：紫微, 流月, 四化
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流月四化中化忌所落星曜的原盘宫位 index。结构变量，方向中性。

**计算**

```
horoscope.monthly.mutagen[3] → 星曜原盘落宫
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 12 | -0.12 | 0.612572 | — |

### Z_MOVE_* · 迁移宫（研究映射：外部市场）

#### `Z_MOVE_001` · 迁移宫主星庙旺和

* **方向（传统规则）**：正向
* **原始值单位**：`score_sum`
* **归一化**：tanh(sum / 1.5)
* **依赖盘面字段**：`ziwei.palace.迁移.major_stars`
* **标签**：紫微, 迁移宫, research_mapping
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

迁移宫主星庙旺等级之和。**研究映射**：迁移宫 → 外部市场/资金流入环境，属本项目假设。

**计算**

```
sum(BRIGHTNESS_SCORE[主星.brightness] for 主星 in 迁移宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.85 | 10 | 0.320321 | 0.315979 | — |

#### `Z_MOVE_002` · 迁移宫煞曜数量

* **方向（传统规则）**：负向
* **原始值单位**：`count`
* **归一化**：-min(count / 2, 1)
* **依赖盘面字段**：`ziwei.palace.迁移`
* **标签**：紫微, 迁移宫, 煞曜
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

迁移宫中六煞星的数量。**研究映射**：迁移宫 → 外部市场环境，故该因子被用作『外部阻力』的代理变量，属本项目假设。**不是传统定论。**

**计算**

```
count(malefic in 迁移宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.4 | 3 | -0.3 | 0.401004 | — |

#### `Z_MOVE_003` · 迁移宫化禄化科同宫

* **方向（传统规则）**：正向
* **原始值单位**：`bool`
* **归一化**：True→+1，False→0
* **依赖盘面字段**：`ziwei.natal_mutagens`
* **标签**：紫微, 迁移宫, 四化
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

生年化禄或化科是否落在迁移宫。**研究映射**：迁移宫 → 外部市场，故此处把化吉入迁移解释为外部环境受化，属本项目假设。

**计算**

```
any(生年四化 in {禄, 科} 落于 迁移宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.25 | 2 | 0.25 | 0.434099 | — |

### Z_MUTAGEN_* · 生年四化

#### `Z_MUTAGEN_001` · 生年化禄落宫位阶

* **方向（传统规则）**：正向
* **原始值单位**：`tier`
* **归一化**：tier 2→+1，1→+0.5，0→0，-1→不可用
* **依赖盘面字段**：`ziwei.natal_mutagens`
* **标签**：紫微, 四化, 禄
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

生年化禄所落宫位在『命-财-官-迁』关键宫集中的位阶。传统认为化禄入命财官迁为吉位。位阶 2=核心位（命财官迁），1=次核心位（福德/田宅），0=其他位，-1=缺失。

**计算**

```
根据 natal_mutagens[禄].palace_name 映射位阶
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.8 | 3 | 0.7 | 0.401004 | — |

#### `Z_MUTAGEN_002` · 生年化权落宫位阶

* **方向（传统规则）**：正向
* **原始值单位**：`tier`
* **归一化**：tier 2→+1，1→+0.5，0→0，-1→不可用
* **依赖盘面字段**：`ziwei.natal_mutagens`
* **标签**：紫微, 四化, 权
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

生年化权所落宫位的位阶（同 Z_MUTAGEN_001 的位阶定义）。

**计算**

```
根据 natal_mutagens[权].palace_name 映射位阶
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.3 | 3 | 0.275 | 0.433375 | — |

#### `Z_MUTAGEN_003` · 生年化科落宫位阶

* **方向（传统规则）**：正向
* **原始值单位**：`tier`
* **归一化**：tier 2→+1，1→+0.5，0→0，-1→不可用
* **依赖盘面字段**：`ziwei.natal_mutagens`
* **标签**：紫微, 四化, 科
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

生年化科所落宫位的位阶（同 Z_MUTAGEN_001 的位阶定义）。

**计算**

```
根据 natal_mutagens[科].palace_name 映射位阶
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.45 | 3 | 0.425 | 0.482742 | — |

#### `Z_MUTAGEN_004` · 生年化忌落宫位阶

* **方向（传统规则）**：负向
* **原始值单位**：`tier`
* **归一化**：tier 2→-1，1→-0.5，0→0，-1→不可用
* **依赖盘面字段**：`ziwei.natal_mutagens`
* **标签**：紫微, 四化, 忌
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

生年化忌所落宫位的位阶。化忌入命财官迁传统视为该位受冲。

**计算**

```
根据 natal_mutagens[忌].palace_name 映射位阶
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.45 | 3 | -0.375 | 0.444819 | — |

#### `Z_MUTAGEN_005` · 化吉入关键宫计数

* **方向（传统规则）**：正向
* **原始值单位**：`count`
* **归一化**：min(count / 2, 1)
* **依赖盘面字段**：`ziwei.natal_mutagens`
* **标签**：紫微, 四化
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

生年化禄/化权/化科三者中，落在『命-财-官-迁』关键宫的数量。

**计算**

```
count(禄权科 落宫 ∈ {命宫, 财帛, 官禄, 迁移})
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.8 | 3 | 0.625 | 0.384204 | — |

#### `Z_MUTAGEN_006` · 化忌入关键宫

* **方向（传统规则）**：负向
* **原始值单位**：`bool`
* **归一化**：True→-1，False→0
* **依赖盘面字段**：`ziwei.natal_mutagens`
* **标签**：紫微, 四化, 忌
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

生年化忌是否落在『命-财-官-迁』任一关键宫。

**计算**

```
natal_mutagens[忌].palace_name ∈ {命宫, 财帛, 官禄, 迁移}
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.3 | 2 | -0.3 | 0.459408 | — |

### Z_TRINE_* · 三方四正

#### `Z_TRINE_001` · 命宫三方四正主星庙旺和

* **方向（传统规则）**：正向
* **原始值单位**：`score_sum`
* **归一化**：tanh(sum / 4)
* **依赖盘面字段**：`ziwei.trine.命宫`
* **标签**：紫微, 三方四正
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

命宫三方四正（命/迁/财/官）四宫主星庙旺等级之和。传统以三方四正的整体格局判断结构强弱。

**计算**

```
sum(BRIGHTNESS_SCORE[主星.brightness] over 四宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 20 | 0.569947 | 0.242519 | — |

#### `Z_TRINE_002` · 命宫三方四正吉曜数

* **方向（传统规则）**：正向
* **原始值单位**：`count`
* **归一化**：min(count / 6, 1)
* **依赖盘面字段**：`ziwei.trine.命宫`
* **标签**：紫微, 三方四正, 吉曜
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

命宫三方四正四宫中吉曜（六吉星 + 禄存 + 天马）的总数。传统认为吉曜汇集则格局得力，本项目仅作为可检验的结构变量。

**计算**

```
count(吉曜 over 四宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.9 | 5 | 0.358333 | 0.225874 | — |

#### `Z_TRINE_003` · 命宫三方四正煞曜数

* **方向（传统规则）**：负向
* **原始值单位**：`count`
* **归一化**：-min(count / 4, 1)
* **依赖盘面字段**：`ziwei.trine.命宫`
* **标签**：紫微, 三方四正, 煞曜
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

命宫三方四正四宫中六煞星（六煞全集）的总数。传统以三方四正的整体格局判断结构强弱，本因子是该判断的一个分量。

**计算**

```
count(煞曜 over 四宫)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.95 | 5 | -0.575 | 0.286856 | — |

#### `Z_TRINE_004` · 命宫三方四正吉煞差

* **方向（传统规则）**：正向
* **原始值单位**：`count_diff`
* **归一化**：tanh(diff / 3)
* **依赖盘面字段**：`ziwei.trine.命宫`
* **标签**：紫微, 三方四正
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

三方四正的吉曜总数与煞曜总数之差，传统『吉凶相抵』概念的量化近似。正值表示吉曜多于煞曜。这是传统规则的看法，不构成对股价的判断。

**计算**

```
吉曜数 - 煞曜数
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.9 | 10 | -0.047896 | 0.603858 | — |

#### `Z_TRINE_005` · 三宫主星总数

* **方向（传统规则）**：中性
* **原始值单位**：`count`
* **归一化**：min(count / 5, 1)
* **依赖盘面字段**：`ziwei.palace`
* **标签**：紫微, 三方四正
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

财帛 / 官禄 / 迁移 三宫的主星总数（不含命宫）。结构性变量，方向中性。

**计算**

```
sum(len(主星) for 宫 ∈ {财帛, 官禄, 迁移})
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 5 | 0.72 | 0.26448 | — |

### Z_YEAR_* · 流年

#### `Z_YEAR_001` · 流年命宫原盘位阶

* **方向（传统规则）**：中性
* **原始值单位**：`palace_index`
* **归一化**：index 映射到 [-1,1]；仅作结构编码，不含吉凶
* **依赖盘面字段**：`ziwei.horoscope.yearly.index`
* **标签**：紫微, 流年
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

该年流年命宫落在原盘的哪一宫（宫位 index）。属于分类/结构变量，方向中性。

**计算**

```
horoscope.yearly.index
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 5 | -0.236364 | 0.645982 | — |

#### `Z_YEAR_002` · 流年命宫主星庙旺和

* **方向（传统规则）**：正向
* **原始值单位**：`score_sum`
* **归一化**：tanh(sum / 1.5)
* **依赖盘面字段**：`ziwei.horoscope.yearly.index`
* **标签**：紫微, 流年
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流年命宫（原盘对应宫位）的主星庙旺等级之和。

**计算**

```
sum(BRIGHTNESS_SCORE[主星.brightness] at palace horoscope.yearly.index)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.93 | 15 | 0.410698 | 0.348521 | — |

#### `Z_YEAR_003` · 流年命宫煞曜数量

* **方向（传统规则）**：负向
* **原始值单位**：`count`
* **归一化**：-min(count / 3, 1)
* **依赖盘面字段**：`ziwei.horoscope.yearly`
* **标签**：紫微, 流年, 煞曜
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流年命宫所在宫位的原局煞曜与流年煞曜（流羊/流陀等）总数。

**计算**

```
count(malefic in 原局宫) + count(流年煞曜)
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.56 | 4 | -0.253333 | 0.267837 | — |

#### `Z_YEAR_004` · 流年化禄落原盘宫

* **方向（传统规则）**：中性
* **原始值单位**：`palace_index`
* **归一化**：index 映射到 [-1,1]；仅作结构编码
* **依赖盘面字段**：`ziwei.horoscope.yearly.mutagen`
* **标签**：紫微, 流年, 四化
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

该年流年四化中化禄所落星曜的原盘宫位 index。结构变量，方向中性（吉凶判断留给 Z_YEAR_006 等带方向的因子）。

**计算**

```
horoscope.yearly.mutagen[0] → 找到该星在原盘的宫位
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 12 | 0.003636 | 0.630617 | — |

#### `Z_YEAR_005` · 流年化忌落原盘宫

* **方向（传统规则）**：中性
* **原始值单位**：`palace_index`
* **归一化**：index 映射到 [-1,1]；仅作结构编码
* **依赖盘面字段**：`ziwei.horoscope.yearly.mutagen`
* **标签**：紫微, 流年, 四化
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

该年流年四化中化忌所落星曜的原盘宫位 index。结构变量，方向中性。

**计算**

```
horoscope.yearly.mutagen[3] → 找到该星在原盘的宫位
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 1.0 | 12 | -0.069091 | 0.655823 | — |

#### `Z_YEAR_006` · 流年命宫与原盘命宫同位

* **方向（传统规则）**：中性
* **原始值单位**：`bool`
* **归一化**：True→+0.5（结构集中），False→0
* **依赖盘面字段**：`ziwei.horoscope.yearly.index`
* **标签**：紫微, 流年
* **观测 rule_version**：`zv1.fwd` / `zv1.rev`

**定义**

流年命宫是否与原盘命宫落在同一宫（太岁重叠）。传统视为该年原局结构被直接引动。

**计算**

```
horoscope.yearly.index == soul_palace_index
```

**规则分语义**：传统紫微斗数规则强度分（0-10）。0 表示中性/无该结构，10 表示该结构在传统紫微中最强。该分数不使用任何历史行情数据，不代表预期收益率，也不代表上涨概率。

**质量审计（真实快照）**

| 样本数 | 空值率 | 激活率 | 不同取值数 | 均值 | 标准差 | 标记 |
|---|---|---|---|---|---|---|
| 200 | 0.0 | 0.08 | 2 | 0.04 | 0.135987 | — |

---

## 3. 质量标记汇总（**只报告，不删除**）

| 标记 | 数量 | 因子 |
|---|---|---|
| `CONSTANT_FACTOR` | 1 | `Z_LIFE_006` |
| `NEAR_CONSTANT_FACTOR` | 0 | — |
| `LOW_DISCRIMINATION_FACTOR` | 1 | `Z_LIFE_006` |
| `ALL_NULL_FACTOR` | 0 | — |

**疑似重复（|Pearson| > 0.98）**

无。

> 审计口径说明：连续型因子（如庙旺和）**不使用** `activation_rate` 判低区分度 —— 它们的取值几乎永远不为 0，用激活率会误判。判定规则见 `src/factors/ziwei/audit.py` 模块文档。

---

## 4. 参考

* [`docs/ziwei-engine.md`](ziwei-engine.md)
* [`docs/calculation-differences-phase2-ziwei.md`](calculation-differences-phase2-ziwei.md)
* [`docs/factor_quality_report.md`](factor_quality_report.md)（Phase 1 因子审计）
